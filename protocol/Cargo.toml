[package]
name = "bolt-rendezvous-protocol"
version = "0.1.0"
edition = "2021"
description = "Canonical signaling message types for the Bolt rendezvous protocol"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
